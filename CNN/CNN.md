1，将string分词变为列表
2.通过字典给列表里面的元素转成编码
3.然后再将编码embed嵌入，嵌入层（`nn.Embedding`）是**模型可学习的参数层**，它把离散的整数 ID，转换成**连续的、低维的、有语义的向量**。

上游：
1.卷积
2.池化（pooling）：将数据进行处理，没那么冗杂，返回滑动窗口中的最大值

下游：
1.拿图像特征去分类器做分类


nn.Conv2d(x,y,z)
1.第一个数字：维度 彩色为3，黑白为1
2.第二个数字：中间卷完后有几个频道
3.第三个数字：卷积核为多少，一般为三
4.第四个数字：stride=1, 步长
5.第五个数字：padding = 1，填充

padding：
input * kernel 的运算是怎么算的：扫过的格子直接乘对应的卷积核再全部相加


微调：
1.加载别人的预训练模型，不用自己搞
2.只训练最后一层的分类

定义分类器模型，定义损失函数和优化器








